# Fill this file in with your relevant credentials
{
  # AWS credentials profile
  awsProfile = 
  # AWS VPC subnet, you'll probably have a default created one in your account
  subnet = 
  # Any security groups, you'll probably need at least one for ssh
  securityGroupIds = 
}
